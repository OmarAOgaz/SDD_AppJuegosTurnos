name: turnos_juegos
description: Turnos Juegos de mesa — LAN multiplayer turn timer.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  shelf: ^1.4.2
  shelf_web_socket: ^3.0.0
  web_socket_channel: ^3.0.2
  bonsoir: ^7.1.4
  flutter_foreground_task: ^9.2.2
  shared_preferences: ^2.3.3
  flutter_riverpod: ^3.3.2
  go_router: ^17.3.0
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/sound_stub.wav
